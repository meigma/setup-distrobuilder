.DS_Store
.moon/
CHANGELOG.md
coverage/
dist/
mise.lock
node_modules/

# Machine-written by release-please (compact JSON); checking it fails every
# release PR. CHANGELOG.md above is exempt for the same reason.
.release-please-manifest.json

# Ephemeral compiled rollup config (rollup --configPlugin); appears only
# mid-build and racing its deletion breaks repo-wide globs.
rollup.config-*.mjs

# AI session protocol files — framework-owned, installed verbatim from ~/code/ai;
# reformatting them would break re-install idempotency.
.session.md
AGENTS.md
CLAUDE.md
